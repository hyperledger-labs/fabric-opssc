apiVersion: v1
kind: Service
metadata:
  name: {{ include "opssc-agent.fullname" . }}
  labels:
    {{- include "opssc-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "opssc-agent.selectorLabels" . | nindent 4 }}
